# 🔐 Variáveis de Ambiente para Produção (Vercel)
# Copie estas variáveis para o dashboard do Vercel

# 🗄️ Banco de Dados PostgreSQL
# Obtenha esta URL no seu provedor (Neon, Supabase, PlanetScale, etc.)
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# 🔑 Chave de Sessão (gere uma nova)
# Execute: openssl rand -base64 32
SESSION_SECRET=sua_chave_super_secreta_aqui

# 🌍 Ambiente
NODE_ENV=production

# 📝 Exemplo de como gerar SESSION_SECRET:
# No terminal: openssl rand -base64 32
# Resultado: dGVzdGVfc2VjcmV0X2tleV9leGFtcGxl

# ⚠️ IMPORTANTE:
# - Nunca commite este arquivo com valores reais
# - Configure estas variáveis no dashboard do Vercel
# - Use valores diferentes para desenvolvimento e produção
